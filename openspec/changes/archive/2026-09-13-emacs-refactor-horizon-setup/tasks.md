## 1. Define the shared macro

- [x] 1.1 Create `emacs/.config/emacs/lisp/init-horizon-types.el` with a `-*- lexical-binding: t; -*-` header, a Commentary block explaining what `prilepp/define-horizon-type` generates and why (mirrors design.md "Decisions"), and `(provide 'init-horizon-types)` at the end. Verify the file loads standalone with `emacs -Q --batch -l emacs/.config/emacs/lisp/init-horizon-types.el` with no errors.
- [x] 1.2 In that file, implement `(defmacro prilepp/define-horizon-type (slug &key directory list-fn find-fn find-prompt ensure-fn default-content link-word link-follow-fn link-complete-fn insert-fn insert-prompt) ...)` that expands to: a `defvar <directory-var>` (named `prilepp/<slug>-directory`), a `defun <list-fn>` (mirrors the existing `directory-files ... "\\.org\\'"` + `file-name-sans-extension` body), a `defun <ensure-fn>` (mirrors the existing `unless (file-exists-p ...)` + `make-directory` + optional seeded content via `with-temp-buffer`/`insert`/`write-file` body, where `default-content` is `nil`, a literal string, or a format-string applied to the entity name), and a `defun <find-fn>` using `find-prompt`. Verify by macro-expanding a call with `M-x pp-macroexpand-last-sexp` (or `macroexpand-1` in `ielm`) and confirming the expansion matches today's hand-written Project functions field-for-field (same body shape, same `completing-read` prompt).
- [x] 1.3 Extend the macro so that when `link-word` is non-nil, it additionally expands to: an `org-link-set-parameters` call (wrapped in `with-eval-after-load 'org`) registering `<link-word>:` with `<link-follow-fn>`/`<link-complete-fn>`, a `defun <link-follow-fn>` (calls the type's ensure-fn), a `defun <link-complete-fn>` (prompts via `completing-read` with `insert-prompt`, returns `"<link-word>:NAME"`), and a `defun <insert-fn>` (prompts via `completing-read` with `insert-prompt`, calls ensure-fn, inserts `[[<link-word>:NAME][NAME]]`). When `link-word` is nil, verify none of these four forms are generated (e.g. `macroexpand-1` on an Actions-shaped call produces only the four forms from 1.2, nothing link-related).

## 2. Re-express Project, Area, Life, Goal, Vision in `init-projects.el`

- [x] 2.1 Replace the hand-written Project block (`prilepp/proj-directory` through `prilepp/org-insert-project-link`) with one `prilepp/define-horizon-type` call using: slug `proj`, directory `"~/notes/horizons/proj/"`, `list-fn prilepp/proj--list-projects`, `find-fn prilepp/proj-find`, `find-prompt "Project: "`, `ensure-fn prilepp/proj-ensure-file`, `default-content "#+TODO: TODO(t) WAITING(w) | DONE(d)\n\n* TODO %s\n"` (name-interpolated), `link-word "project"`, `link-follow-fn prilepp/org-project-follow`, `link-complete-fn prilepp/org-project-complete`, `insert-fn prilepp/org-insert-project-link`, `insert-prompt "Project: "`. Verify `M-x prilepp/proj-find`, `[[project:...]]` link follow/complete, and `M-x prilepp/org-insert-project-link` all still work exactly as before (same prompts, same file paths, same seeded content for a brand-new project name).
- [x] 2.2 Replace the hand-written Area block with one macro call using: slug `aor`, directory `"~/notes/horizons/aor/"`, `list-fn prilepp/aor--list-aors`, `find-fn prilepp/aor-find`, `find-prompt "AoR: "` (note: this prompt differs from Area's other prompts - preserve it exactly, do not unify it with "Area: "), `ensure-fn prilepp/aor-ensure-file`, `default-content nil`, `link-word "area"`, `link-follow-fn prilepp/org-area-follow`, `link-complete-fn prilepp/org-area-complete`, `insert-fn prilepp/org-insert-area-link`, `insert-prompt "Area: "`. Verify `prilepp/aor-find` still prompts "AoR: " while `[[area:...]]` completion and `prilepp/org-insert-area-link` still prompt "Area: ", and that `prilepp/routine--candidates` (which reads `prilepp/aor-directory` directly) still works unchanged.
- [x] 2.3 Replace the hand-written Life block with one macro call using: slug `life`, directory `"~/notes/horizons/life/"`, `list-fn prilepp/life--list-entries`, `find-fn prilepp/life-find`, `find-prompt "Life: "`, `ensure-fn prilepp/life-ensure-file`, `default-content nil`, `link-word "life"`, `link-follow-fn prilepp/org-life-follow`, `link-complete-fn prilepp/org-life-complete`, `insert-fn prilepp/org-insert-life-link`, `insert-prompt "Life: "`. Verify `prilepp/life-find`, `[[life:...]]` follow/complete, and `prilepp/org-insert-life-link` behave as before.
- [x] 2.4 Replace the hand-written Goal block with one macro call using: slug `goals`, directory `"~/notes/horizons/goals/"`, `list-fn prilepp/goals--list-entries`, `find-fn prilepp/goals-find`, `find-prompt "Goal: "`, `ensure-fn prilepp/goals-ensure-file`, `default-content nil`, `link-word "goal"`, `link-follow-fn prilepp/org-goal-follow`, `link-complete-fn prilepp/org-goal-complete`, `insert-fn prilepp/org-insert-goal-link`, `insert-prompt "Goal: "`. Verify `prilepp/goals-find`, `[[goal:...]]` follow/complete, and `prilepp/org-insert-goal-link` behave as before.
- [x] 2.5 Replace the hand-written Vision block with one macro call using: slug `vision`, directory `"~/notes/horizons/vision/"`, `list-fn prilepp/vision--list-entries`, `find-fn prilepp/vision-find`, `find-prompt "Vision: "`, `ensure-fn prilepp/vision-ensure-file`, `default-content nil`, `link-word "vision"`, `link-follow-fn prilepp/org-vision-follow`, `link-complete-fn prilepp/org-vision-complete`, `insert-fn prilepp/org-insert-vision-link`, `insert-prompt "Vision: "`. Verify `prilepp/vision-find`, `[[vision:...]]` follow/complete, and `prilepp/org-insert-vision-link` behave as before.
- [x] 2.6 Keep `prilepp/routine--candidates`, `prilepp/routine-jump`, and `prilepp/routine-insert-new` in `init-projects.el` untouched (they are not part of the duplicated horizon-type shape). Verify `prilepp/routine-find` still works after 2.1-2.5.

## 3. Re-express Actions in `init-files.el`

- [x] 3.1 Replace the hand-written `prilepp/actions-directory`/`prilepp/actions--list-contexts`/`prilepp/actions-ensure-file`/`prilepp/actions-find` block with one `prilepp/define-horizon-type` call using: slug `actions`, directory `"~/notes/horizons/actions/"`, `list-fn prilepp/actions--list-contexts`, `find-fn prilepp/actions-find`, `find-prompt "Context: "`, `ensure-fn prilepp/actions-ensure-file`, `default-content "#+TODO: TODO(t) WAITING(w) | DONE(d)\n\n"` (static, not name-interpolated), `link-word nil` (per design.md Non-Goals - do not add an `action:` link type or insert-link command in this change). Verify `prilepp/actions-find`, the `org-agenda-custom-commands` deadlines view, `prilepp/action-move-to-context`, and `prilepp/entity-sources`' Action source (which calls `prilepp/actions-ensure-file`/`prilepp/actions--list-contexts` directly) all still work unchanged.

## 4. Wire up load order

- [x] 4.1 In `emacs/.config/emacs/init.el`, add `(require 'init-horizon-types)` immediately after `(require 'init-org)` and before `(require 'init-files)`. Verify `emacs -Q --batch -l emacs/.config/emacs/init.el` (or a full interactive Emacs restart with this config) loads with no "macro ... not known to be defined" byte-compiler warnings and no errors.

## 5. Manual verification

- [ ] 5.1 Restart Emacs with this config and confirm `C-c n` still lists all Project/Area/Life/Goal/Vision/Action candidates identically to before the refactor (same names, same grouping/narrow keys) - `prilepp/entity-sources` is untouched, so this should need no code changes, only confirms nothing broke.
- [ ] 5.2 For each of Project/Area/Life/Goal/Vision: create one brand-new entity via `C-c n` (typing a new name under that type's narrow key) and confirm the created file's path and seeded content exactly match what the pre-refactor code would have produced (empty for Area/Life/Goal/Vision, `"#+TODO: TODO(t) WAITING(w) | DONE(d)\n\n* TODO <name>\n"` for Project).
- [ ] 5.3 For each of Project/Area/Life/Goal/Vision: insert a link via `C-c n` `C-i` on an existing entity and confirm the inserted `[[<word>:NAME][NAME]]` link still resolves (place point on it and press `C-c C-o` / follow) to the correct file.
- [ ] 5.4 Confirm `prilepp/action-move-to-context` (`C-c m`) and the deadlines agenda view (`C-c v d`) still work against `~/notes/horizons/actions/`.
- [x] 5.5 Run `git diff` over `init-projects.el`, `init-files.el`, `init-horizon-types.el`, and `init.el` and confirm no unrelated files changed, then confirm the diff removes roughly the ~230 lines of duplicated boilerplate described in proposal.md's "Why".
